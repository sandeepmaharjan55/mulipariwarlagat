@model IEnumerable<Woda_test.Models.table_address>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="pull-right">
    @using (Html.BeginForm("Index", "address", FormMethod.Get))
    {
        @Html.TextBox("q", "", new { placeholder = "Search..." });

        <button type="submit" class="btn btn-primary">GO</button>

    }
</div>
<br />
<table class="table">
    <tr>
        <th>
            <a href="~/address?order=oda">
                वडा नं.
                </a>
</th>
        <th><a href="~/address?order=sabikgbs">
                साविक गा.वि.स.
</a>
   
</th>
        <th>
          
            साविक वडा
            
        </th>
        <th>
            <a href="~/address?order=tole">
                तोल
            </a>
        </th>
        <th>
            घर नं.  </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Oda_no)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sabik_gabisa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sabik_Oda)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tole)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.table_house_senior_details.Home_no
       )
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Add_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Add_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Add_id })
        </td>
    </tr>
}

</table>
